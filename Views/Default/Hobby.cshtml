@using CV_MVC.Models.Entity
@model List<TBLHOBBY>

<div class="resume-section-content">
    <h2 class="mb-5">Interests</h2>
    @foreach (var item in Model)
    {
        <p> @item.Detail1 </p>
        <p class="mb-0">@item.Detail2</p>
    }
</div>
